package stat

const stat_examples = `Example 1: Print a remote object's metadata

         s6cmd stat s3://bucket/prefix/object

Example 2: Check if a remote bucket exists

         s6cmd stat s3://bucket
`
